data:
  env:
    GEMINI_API_KEY:
      description: API key for Google Gemini
      traits:
        - secret
        - required

  args:
    TBD: TBD

  const:
    files: | # njk
      <files root="{{ os.pwd }}">
      {% for entry in os.find_text_files( os.pwd ) %}
        <file path="{{ entry.relpath }}">{{ xml.cdata(entry.content) }}</file>
      {% endfor %}
      </files>

tools:
  # os_read_file: 
  #   ts: | # typescript
  #     return function os_read_file() {
  #       // TODO: Implement, document, fallbacks too
  #     }
  
toolsets:
  default: 
    - os_read_file


